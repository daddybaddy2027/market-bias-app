# Supabase function config
# PayPal webhooks do not send Supabase JWT, so JWT verification must be disabled.

[functions.paypal-webhook]
verify_jwt = false

# Public checkout configuration returns only the PayPal client ID and plan IDs.
# It never returns client secrets or webhook identifiers.
[functions.paypal-public-config]
verify_jwt = false
